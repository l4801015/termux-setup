sh -c 'G="\033[32m";Y="\033[33m";R="\033[31m";X="\033[0m";check_storage() { [ -d "$HOME/storage/downloads" ] && [ -w "$HOME/storage/downloads" ];};if check_storage; then printf "%b\n" "${G}✓ Storage active!$X";else printf "%b\n" "${Y}⚠ Starting setup...$X";termux-setup-storage;T=60;S=$(date +%s);while :;do N=$(date +%s);E=$((N-S));[ $E -ge $T ] && printf "\n%b\n" "${R}✗ Timeout!$X" && exit 1;check_storage && printf "\n%b\n" "${G}✓ Activated!$X" && exit 0;printf "\r${Y}Time remaining: %02d:%02d$X" $(((T-E)/60)) $(((T-E)%60));sleep 1;done;fi'
